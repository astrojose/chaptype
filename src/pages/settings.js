import React from 'react';

const Settings = () => {
  return (
    <section className="page state-page">
      <p className="eyebrow">Settings</p>
      <h1>Settings will grow with future learning modes.</h1>
      <p className="page-summary">
        This route is now part of the application shell so preferences, filters, and
        accessibility options can be added without changing the routing foundation.
      </p>
    </section>
  );
};

export default Settings;
